fastapi
uvicorn
sqlalchemy
python-multipart
python-jose[cryptography]   # import: from jose import jwt
bcrypt                      # import: bcrypt.hashpw / bcrypt.checkpw (replaces passlib)
python-dotenv                # import: from dotenv import load_dotenv
requests                     # for Mailgun email service
pyyaml>=6.0                  # model catalogue manifest (catalog/models.yaml)
google-auth>=2.0             # Google OAuth ID token verification

# Dev / test dependencies
pytest>=7.0
httpx>=0.25