# GPU Worker Daemon — Dependencies
# Install: pip install -r requirements.txt

# HTTP client (async)
httpx>=0.27.0

# Data validation
pydantic>=2.0.0
jsonschema>=4.0.0

# Config file parsing
pyyaml>=6.0

# Apple Silicon GPU memory reporting via Metal. macOS-only — pip skips this
# entirely on Linux/Windows workers. Without it the daemon falls back to a
# conservative fraction of total RAM (see hardware._apple_vram_gb).
pyobjc-framework-Metal>=10.0 ; sys_platform == "darwin"

# ── Dev/Test dependencies ────────────────────────────────────
# Uncomment for development:
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# fastapi>=0.115.0      # for mock backend
# uvicorn>=0.30.0       # for mock backend
# python-multipart>=0.0.9  # for file uploads in mock backend
